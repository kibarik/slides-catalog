# Каталог полностью открыт для индексации и для превью-ботов мессенджеров
# (facebookexternalhit, meta-externalagent, TelegramBot, Slackbot и прочих).
# Файл существует, чтобы на запрос /robots.txt не отдавалась HTML-страница 404:
# явное разрешение снимает вопрос «превью не собралось из-за robots.txt».

User-agent: *
Allow: /
