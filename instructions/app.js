const id=document.body.dataset.guide,guide=window.GUIDES[id],root=document.querySelector('#app');
function command(value){return `<pre class="command"><code>${value}</code><button class="copy-command" type="button" data-copy="${encodeURIComponent(value)}">Копировать</button></pre>`}
function steps(items){return `<ol class="steps">${items.map(x=>`<li>${x.text}${x.code?command(x.code):''}</li>`).join('')}</ol>`}
function bind(platform){
  document.querySelectorAll('[data-platform]').forEach(b=>b.onclick=()=>{localStorage.setItem('guide-platform',b.dataset.platform);render(b.dataset.platform)});
  document.querySelectorAll('[data-copy]').forEach(button=>button.onclick=async()=>{await navigator.clipboard.writeText(decodeURIComponent(button.dataset.copy));button.textContent='Скопировано';setTimeout(()=>button.textContent='Копировать',1400)});
}
function renderLongread(platform){
  const items=guide.platforms[platform];
  root.innerHTML=`<div class="shell"><header class="top"><a class="brand" href="../">slides.aleksishmanov.ru/instructions</a><span class="step">${guide.number}</span></header><section class="hero"><div><h1>${guide.title}</h1><p>${guide.lead}</p><div class="meta-row"><span>10–15 минут</span><span>${platform}</span><span>для новичка</span></div></div><aside class="status"><strong>Результат</strong><p>Команда <code>claude --version</code> показывает установленную версию без ошибки.</p></aside></section><main class="article-layout"><article class="article"><div class="lead-block"><strong>Что мы делаем</strong>Сначала проверяем Node.js, затем устанавливаем Claude Code и убеждаемся, что команда доступна в новом окне терминала.</div><nav class="platforms" aria-label="Операционная система">${Object.keys(guide.platforms).map(x=>`<button data-platform="${x}" class="${x===platform?'active':''}">${x}</button>`).join('')}</nav><figure class="article-visual"><img src="${guide.image}" alt="${guide.imageAlt}"><figcaption>Официальная инструкция Z.ai. Интерфейс страницы может измениться, команды ниже сверены на 17 сентября 2026 года.</figcaption></figure><h2 id="before">Перед началом</h2><p>Закройте старые окна терминала после установки Node.js и откройте новое. Так система перечитает переменную PATH. Все команды вводите по одной строке и нажимайте Enter.</p><div class="warning"><strong>Не вставляйте API-ключи на этом шаге.</strong> Здесь устанавливается только программа Claude Code.</div><h2 id="install">Установка на ${platform}</h2>${steps(items)}<h2 id="check">Проверка результата</h2><p>Откройте новое окно Terminal или PowerShell и выполните команду:</p>${command('claude --version')}<div class="check"><strong>Всё получилось</strong>Вы видите номер версии Claude Code. Авторизация в Anthropic сейчас не нужна: модель GLM подключим через Z.ai на пятом шаге.</div><h2 id="problems">Если команда не работает</h2><div class="trouble"><strong><code>node</code> или <code>npm</code> не найдены</strong><p>Node.js не установился или терминал ещё не обновил PATH. Перезапустите терминал и снова выполните <code>node --version</code>.</p></div><div class="trouble"><strong>Windows не видит <code>winget</code></strong><p>Установите Node.js LTS с официального сайта nodejs.org, затем откройте новое окно PowerShell.</p></div><div class="trouble"><strong>macOS или Linux показывает EACCES</strong><p>Не повторяйте команду много раз. Официальный Z.ai рекомендует установить Node.js через nvm, чтобы избежать глобальных ошибок доступа.</p></div><div class="trouble"><strong><code>claude</code> не найден после npm install</strong><p>Закройте терминал, откройте снова и повторите <code>claude --version</code>. Если ошибка осталась, выполните <code>npm config get prefix</code> и проверьте, что этот путь добавлен в PATH.</p></div><a class="next-step" href="../zai-registration/">Дальше: регистрация в Z.ai →</a></article><aside class="toc"><strong>На этой странице</strong><a href="#before">Перед началом</a><a href="#install">Установка</a><a href="#check">Проверка</a><a href="#problems">Частые ошибки</a><workshop-poll guide="${id}"></workshop-poll></aside></main><footer class="sources"><strong>Официальные источники:</strong>${guide.sources.map(url=>`<a href="${url}">${url}</a>`).join('')}</footer></div>`;
  bind(platform);
}
function renderDefault(platform){
  const items=guide.platforms?guide.platforms[platform]:guide.steps;
  root.innerHTML=`<div class="shell"><header class="top"><a class="brand" href="../">slides.aleksishmanov.ru/instructions</a><span class="step">${guide.number}</span></header><section class="hero"><div><h1>${guide.title}</h1><p>${guide.lead}</p></div><aside class="status"><strong>Отметьте результат</strong><p>Кнопки ниже сохраняют ваш ответ на этом устройстве. Общая статистика пока в прототипе.</p><div class="poll-wrap"><workshop-poll guide="${id}"></workshop-poll></div></aside></section>${guide.platforms?`<nav class="platforms" aria-label="Операционная система">${Object.keys(guide.platforms).map(x=>`<button data-platform="${x}" class="${x===platform?'active':''}">${x}</button>`).join('')}</nav>`:''}${guide.warning?`<div class="warning">${guide.warning}</div>`:''}<main class="content"><div>${steps(items)}</div><aside class="visual"><img src="${guide.image}" alt="${guide.imageAlt}"><p>${guide.imageAlt}. Снимок используется как ориентир, интерфейс сайта может измениться.</p></aside></main><footer class="sources"><strong>Проверить актуальные шаги:</strong>${guide.sources.map(url=>`<a href="${url}">${url}</a>`).join('')}</footer></div>`;
  bind(platform);
}
function render(platform){if(id==='claude-code')renderLongread(platform);else renderDefault(platform)}
if(!guide){root.innerHTML='<div class="shell"><h1>Инструкция не найдена</h1><a href="../">Вернуться к списку</a></div>'}else{render(guide.platforms?(localStorage.getItem('guide-platform')||'macOS'):'')}
